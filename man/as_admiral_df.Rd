% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admiral_df.R
\name{as_admiral_df}
\alias{as_admiral_df}
\title{Tag a Dataset with the \code{admiral_df} Class}
\usage{
as_admiral_df(dataset)
}
\arguments{
\item{dataset}{A data frame (or \code{NULL})

\describe{
\item{Default value}{none}
}}
}
\value{
If \code{dataset} is \code{NULL}, \code{NULL} is returned unchanged. Otherwise
\code{dataset} with \code{"admiral_df"} prepended to its class attribute.
}
\description{
Adds the \code{admiral_df} class to a data frame (unless it is already
present), preserving the existing classes such as \code{tbl_df}/\code{data.frame}.
This is the tagging primitive that admiral's \verb{derive_*()} and related
dataset-producing functions apply to their output, so that a dataset
built with admiral can be recognized as such downstream (e.g. by a
future dedicated \code{summary()} method).
}
\examples{
library(tibble)

adsl <- tribble(
  ~USUBJID, ~AGE,
  "1",      63,
  "2",      71
)

class(as_admiral_df(adsl))
}
\seealso{
Utilities used within Derivation functions:
\code{\link[=extract_unit]{extract_unit()}},
\code{\link[=get_flagged_records]{get_flagged_records()}},
\code{\link[=get_not_mapped]{get_not_mapped()}},
\code{\link[=get_vars_query]{get_vars_query()}}
}
\concept{utils_help}
\keyword{utils_help}
