% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admiral_df.R
\name{check_admiral_df}
\alias{check_admiral_df}
\title{Check the \code{admiral_df} Tag of a Dataset}
\usage{
check_admiral_df(dataset)
}
\arguments{
\item{dataset}{A data frame

\describe{
\item{Default value}{none}
}}
}
\value{
An object of class \code{admiral_df_check} (a named list) which is printed by
\code{\link[=print.admiral_df_check]{print.admiral_df_check()}}, with elements:
\itemize{
\item \code{is_admiral_df} -- whether the \code{admiral_df} class is present.
\item \code{keys} -- the key variables which would be used by
\code{\link[=summary.admiral_df]{summary.admiral_df()}}, i.e. the declared keys still present in the
dataset, or the inferred ones. May be zero-length.
\item \code{key_source} -- \code{"declared"}, \code{"inferred"}, or \code{"none"}.
\item \code{declared_keys} -- the \code{"admiral_keys"} attribute as stored, or \code{NULL}.
\item \code{stale_keys} -- declared keys which are not in the dataset.
\item \code{structure_checked} -- whether \code{\link[=summary.admiral_df]{summary.admiral_df()}} would check the
record structure, i.e. report on one record per key.
}
}
\description{
Reports what is left of the \code{admiral_df} tag on a dataset: whether the class
is still present, which key variables are declared (or would be inferred),
and whether those keys are still in the dataset. Use it before relying on
\code{\link[=summary.admiral_df]{summary.admiral_df()}}, or after an operation which may have dropped the tag.
}
\details{
The \code{admiral_df} class and the \code{"admiral_keys"} attribute are preserved
independently, so a dataset can carry one without the other and each case
behaves differently:
\itemize{
\item \strong{both present} -- the record structure is checked against the declared
keys.
\item \strong{class only} -- \code{\link[=summary.admiral_df]{summary.admiral_df()}} falls back to
\code{\link[=infer_admiral_keys]{infer_admiral_keys()}}, so the record structure is still checked, but
against a guessed key (reported as \code{key_source = "inferred"}).
\item \strong{class only, but \code{"admiral_keys"} set to a zero-length vector} -- the
inference fallback is skipped \emph{and} no key is left to check, so the
record structure is not checked at all. \code{set_admiral_keys()} produces
this when the specification defines no key variables.
\item \strong{keys only} -- \code{summary()} no longer dispatches to
\code{\link[=summary.admiral_df]{summary.admiral_df()}} and the diagnostic is silently lost. Re-tagging
with \code{as_admiral_df(dataset)} restores the class but keeps the existing
(possibly stale) attribute; pass \code{keys} explicitly to replace it.
}

Because keys are stored as plain variable names, \code{dplyr::rename()} and
\code{dplyr::select()} can leave them pointing at variables which are no longer
there. Those are reported in \code{stale_keys}, and the record structure is then
checked against fewer variables than declared (or not at all).
}
\seealso{
\code{\link[=as_admiral_df]{as_admiral_df()}}, \code{\link[=set_admiral_keys]{set_admiral_keys()}}, \code{\link[=summary.admiral_df]{summary.admiral_df()}}

Utilities for printing:
\code{\link[=print.adam_templates]{print.adam_templates()}},
\code{\link[=print.admiral_df_check]{print.admiral_df_check()}},
\code{\link[=print.duplicates]{print.duplicates()}},
\code{\link[=print.source]{print.source()}},
\code{\link[=print.summary_admiral_df]{print.summary_admiral_df()}},
\code{\link[=print_named_list]{print_named_list()}},
\code{\link[=summary.admiral_df]{summary.admiral_df()}}
}
\concept{utils_print}
\keyword{utils_print}
\section{Examples}{
\subsection{Checking the tag before relying on it}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(tibble)

advs <- as_admiral_df(
  tribble(
    ~USUBJID, ~PARAMCD, ~AVAL,
    "1",      "DIABP",     51,
    "1",      "SYSBP",    121
  ),
  keys = c("USUBJID", "PARAMCD")
)

check_admiral_df(advs)
#> ── admiral_df tag check ────────────────────────────────────────────────────────
#>   is an admiral_df:   TRUE
#>   key variables:      USUBJID, PARAMCD
#>   keys present:       all present
#>   structure checked:  yes -- one record per USUBJID, PARAMCD (declared)}\if{html}{\out{</div>}}}
\subsection{Keys which no longer match the dataset}{

The keys are stored as plain variable names, so they are not updated by
\code{dplyr::rename()}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{check_admiral_df(dplyr::rename(advs, SUBJID = USUBJID))
#> ── admiral_df tag check ────────────────────────────────────────────────────────
#>   is an admiral_df:   TRUE
#>   key variables:      USUBJID, PARAMCD
#>   keys present:       stale: USUBJID missing; checked on PARAMCD only
#>   structure checked:  yes -- one record per PARAMCD (declared)}\if{html}{\out{</div>}}}
\subsection{Using the result programmatically}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (!check_admiral_df(advs)$structure_checked) {
  print("re-tag before summarizing")
}}\if{html}{\out{</div>}}}}

