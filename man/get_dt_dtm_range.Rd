% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_vars_dt_dtm_utils.R
\name{get_dt_dtm_range}
\alias{get_dt_dtm_range}
\title{Get range of possible date / datetime}
\usage{
get_dt_dtm_range(dtc, date_imputation = "first", time_imputation = NULL)
}
\arguments{
\item{dtc}{A character vector of dates in ISO 8601 format
(e.g., \code{"2022-12-15"}, \code{"2022-12"}, \code{"2022"}).
Partial dates are allowed.

\describe{
\item{Default value}{none}
}}

\item{date_imputation}{A string specifying the imputation strategy to apply to
incomplete dates.
Accepts \code{"first"} (default) or \code{"last"} to impute to the first or last possible date,
respectively.

\describe{
\item{Default value}{\code{"first"}}
}}
}
\value{
A character vector of fully imputed dates in \code{"YYYY-MM-DD"} format.
}
\description{
Internal helper function to convert a character vector of (possibly partial) dates (\code{dtc})
into complete dates based on a specified imputation rule (\code{date_imputation}).
}
\details{
This function is a simplified version of the \code{impute_dtc_dt()} function that
does not throw an error when having no min_dates nor max_dates specified when
\code{date_imputation = "Y"}.
The function performs the following steps:
\itemize{
\item Validates and parses the input date strings.
\item Determines which components (year, month, day) are missing.
\item Applies the selected imputation rule (\code{"first"} or \code{"last"}) to fill in
missing components.
\item Returns a fully qualified date or \code{NA} if imputation cannot be performed.
}
}
\examples{
# Impute date range for dates with 'first' date imputation
dtc_dates <- c("2020-02-29", "2021-03")
imputed_dates_first <- get_dt_dtm_range(dtc_dates, date_imputation = "first")
print(imputed_dates_first)

# Impute date range for dates with 'last' date imputation
imputed_dates_last <- get_dt_dtm_range(dtc_dates, date_imputation = "last")
print(imputed_dates_last)

# Impute datetime range with 'first' time imputation
dtc_datetimes <- c("2020-02-29T12:00", "2021-03T14:30")
imputed_datetimes_first <- get_dt_dtm_range(dtc_datetimes,
                                            date_imputation = "first",
                                            time_imputation = "first")
print(imputed_datetimes_first)

# Impute datetime range with 'last' time imputation
imputed_datetimes_last <- get_dt_dtm_range(dtc_datetimes,
                                           date_imputation = "first",
                                           time_imputation = "last")
print(imputed_datetimes_last)

# Edge case: Return empty character vector for empty input
imputed_empty <- get_dt_dtm_range(character(0), date_imputation = "first")
print(imputed_empty)

}
\keyword{internal}
