% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admiral_df.R
\name{set_admiral_keys}
\alias{set_admiral_keys}
\title{Tag a Dataset with its Key Variables from a \code{metacore} Specification}
\usage{
set_admiral_keys(dataset, metacore, dataset_name = NULL)
}
\arguments{
\item{dataset}{A data frame

\describe{
\item{Default value}{none}
}}

\item{metacore}{A \code{{metacore}} object, see \code{\link[=get_admiral_keys]{get_admiral_keys()}}

\describe{
\item{Default value}{none}
}}

\item{dataset_name}{The dataset name, see \code{\link[=get_admiral_keys]{get_admiral_keys()}}

\describe{
\item{Default value}{\code{NULL}}
}}
}
\value{
\code{dataset} with an \code{"admiral_keys"} attribute, an \code{"admiral_ds_name"}
attribute (shown in the heading of \code{\link[=summary.admiral_df]{summary.admiral_df()}}), and the
\code{admiral_df} class.
}
\description{
Convenience wrapper that extracts the key variables for \code{dataset_name} from a
\code{{metacore}} specification (via \code{\link[=get_admiral_keys]{get_admiral_keys()}}) and stores them on
\code{dataset} in the \code{"admiral_keys"} attribute, also tagging it as an
\code{admiral_df} (see \code{\link[=as_admiral_df]{as_admiral_df()}}). \code{\link[=summary.admiral_df]{summary.admiral_df()}} uses this
attribute to check the record structure, i.e. that the dataset really does
have one record per key.
}
\details{
The core \code{{dplyr}} verbs (\code{mutate()}, \code{filter()}, \code{arrange()}, \code{select()},
\code{rename()}, \code{slice()}, \code{distinct()}, joins where \code{dataset} is \code{x}, ...)
restore data-frame attributes, so the keys and the \code{admiral_df} class
normally survive a derivation pipeline. They are \emph{not} preserved by
operations which build a new data frame, in particular:
\itemize{
\item \code{summarise()}, \code{group_by()}/\code{ungroup()} and \code{rowwise()},
\item \code{tidyr::pivot_longer()}/\code{pivot_wider()}, \code{nest()}/\code{unnest()},
\code{complete()},
\item \code{bind_rows()} when \code{dataset} is not the \emph{first} argument,
\item joins where \code{dataset} is the \code{y} (rather than the \code{x}) argument.
}

Call this function afterwards (or re-apply it as needed) if the dataset
passes through any of those.

Because the keys are stored as plain variable names, they are not updated
by \code{rename()} and not removed by \code{select()}. \code{\link[=summary.admiral_df]{summary.admiral_df()}} warns
when keys are no longer present in the dataset.
}
\seealso{
Utilities used within Derivation functions:
\code{\link[=extract_unit]{extract_unit()}},
\code{\link[=get_admiral_keys]{get_admiral_keys()}},
\code{\link[=get_flagged_records]{get_flagged_records()}},
\code{\link[=get_not_mapped]{get_not_mapped()}},
\code{\link[=get_vars_query]{get_vars_query()}}
}
\concept{utils_help}
\keyword{utils_help}
