% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admiral_df.R
\name{get_admiral_keys}
\alias{get_admiral_keys}
\title{Extract the Key Variables of a Dataset from a \code{metacore} Specification}
\usage{
get_admiral_keys(metacore, dataset_name = NULL)
}
\arguments{
\item{metacore}{A \code{{metacore}} object (of class \code{Metacore}), typically created
by \code{metacore::spec_to_metacore()} or \code{metacore::metacore()}.

\describe{
\item{Default value}{none}
}}

\item{dataset_name}{The name of the dataset whose keys should be extracted,
e.g. \code{"ADVS"}. If the \code{metacore} object describes a single dataset (for
example after \code{metacore::select_dataset()}), this may be left \code{NULL} and the
sole dataset is used.

\describe{
\item{Default value}{\code{NULL}}
}}
}
\value{
A character vector of key variable names in key order. If the dataset has no
key variables defined in the specification, a zero-length character vector is
returned (with a warning).
}
\description{
Reads the key variables (the variables the dataset should have one record
per) for a given dataset from a \code{{metacore}} specification object. The keys
are taken from the \code{key_seq} column of the \code{ds_vars} table and returned in
key order. These can be used, e.g., to check whether a derivation preserved
the expected record structure (see \code{\link[=summary.admiral_df]{summary.admiral_df()}}).
}
\details{
The keys themselves are retrieved with \code{metacore::get_keys()}, so admiral
tracks \code{{metacore}}'s own definition of the key variables (this function is a
thin wrapper that returns the key variable names as a character vector and
adds a \code{dataset_name = NULL} convenience for single-dataset specs).

\code{{metacore}} is only needed when this function is called; it is a suggested,
not a hard, dependency of admiral. If the package is not installed an
informative error (with an install prompt) is raised.
}
\examples{
# extracting the keys defined in an ADaM specification
if (requireNamespace("metacore", quietly = TRUE)) {
  load(metacore::metacore_example("pilot_ADaM.rda"))
  get_admiral_keys(metacore, "ADSL")
}
}
\seealso{
Utilities used within Derivation functions:
\code{\link[=extract_unit]{extract_unit()}},
\code{\link[=get_flagged_records]{get_flagged_records()}},
\code{\link[=get_not_mapped]{get_not_mapped()}},
\code{\link[=get_vars_query]{get_vars_query()}},
\code{\link[=set_admiral_keys]{set_admiral_keys()}}
}
\concept{utils_help}
\keyword{utils_help}
