name: Test Document Command

on:
  issue_comment:
    types: [created]

jobs:
  test-document:
    runs-on: ubuntu-latest
    steps:
      - name: Debug Event
        run: echo "${{ toJson(github.event) }}"
      - name: Success Message
        run: echo "Triggering /document command!"
